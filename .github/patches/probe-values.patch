diff --git a/.github/workflows/fabricator.yml b/.github/workflows/fabricator.yml
index e98e6ac..2c1b47f 100644
--- a/.github/workflows/fabricator.yml
+++ b/.github/workflows/fabricator.yml
@@ -203,10 +203,13 @@ jobs:
           RELEASE: ${{ toJSON(matrix.order.release) }}
         run: |
           pattern="$(jq -r '.version.pattern' <<<"$RELEASE")"
+          # Each variable the order names gets its own random throwaway value.
+          # They exist only so the image will start; nothing reaches the
+          # probe, and nothing password-shaped is written in the repository.
           envs=()
-          while IFS= read -r kv; do
-            envs+=(-e "$kv")
-          done < <(jq -r '.version.env | to_entries[] | "\(.key)=\(.value)"' <<<"$RELEASE")
+          while IFS= read -r name; do
+            envs+=(-e "${name}=probe-$(od -An -N8 -tx1 /dev/urandom | tr -d ' \n')")
+          done < <(jq -r '.version.env[]' <<<"$RELEASE")
           docker run -d --name version-probe "${envs[@]}" "${IMAGE}@${DIGEST}" >/dev/null
           version=""
           for _ in $(seq 1 "${PROBE_ATTEMPTS:-60}"); do
diff --git a/scripts/work-orders.json b/scripts/work-orders.json
index 05092d2..66ec4c9 100644
--- a/scripts/work-orders.json
+++ b/scripts/work-orders.json
@@ -15,7 +15,8 @@
     "release: the production overlay and the module it builds on, which are",
     "staged with the new image digest and published as",
     "ghcr.io/<repository>-<name>-release:<version>; and how the version is read -",
-    "the environment the image needs in order to start, and the sed pattern that",
+    "the names of the environment variables the image needs set in order to",
+    "start, each given a random throwaway value, and the sed pattern that",
     "captures the version from what it prints. The version is what the built",
     "thing says it is, plus a counter for rebuilds of the same version: 1.0.15-1,",
     "then 1.0.15-2. Nothing here is the fabricator's knowledge; it follows the order."
@@ -32,11 +33,7 @@
         "overlay": "environments/production/applications/valheim",
         "module": "modules/applications/valheim",
         "version": {
-          "env": {
-            "VALHEIM_SERVER_NAME": "version-probe",
-            "VALHEIM_WORLD_NAME": "version-probe",
-            "VALHEIM_PASSWORD": "probe-only-9"
-          },
+          "env": ["VALHEIM_PASSWORD", "VALHEIM_SERVER_NAME", "VALHEIM_WORLD_NAME"],
           "pattern": "Valheim version: [a-z]*-\\{0,1\\}\\([0-9][0-9.]*\\)"
         }
       }
diff --git a/tests/go/repo/fabricator_test.go b/tests/go/repo/fabricator_test.go
index f8ead85..06c348c 100644
--- a/tests/go/repo/fabricator_test.go
+++ b/tests/go/repo/fabricator_test.go
@@ -425,8 +425,8 @@ func TestEveryReleaseOrderIsOneTheFabricatorCanFollow(t *testing.T) {
 				Overlay string `json:"overlay"`
 				Module  string `json:"module"`
 				Version struct {
-					Env     map[string]string `json:"env"`
-					Pattern string            `json:"pattern"`
+					Env     []string `json:"env"`
+					Pattern string   `json:"pattern"`
 				} `json:"version"`
 			} `json:"release"`
 		} `json:"orders"`
@@ -447,6 +447,9 @@ func TestEveryReleaseOrderIsOneTheFabricatorCanFollow(t *testing.T) {
 		if _, err := os.Stat(filepath.Join(repoRoot(t), o.Release.Module)); err != nil {
 			t.Errorf("%s: the release module %s does not exist: %v", o.Name, o.Release.Module, err)
 		}
+		if len(o.Release.Version.Env) == 0 {
+			t.Errorf("%s: the release order names no environment variables, so the probe starts an image that may refuse to run", o.Name)
+		}
 		if strings.Count(o.Release.Version.Pattern, `\(`) != 1 {
 			t.Errorf("%s: the version pattern must capture exactly one group, the version: %q", o.Name, o.Release.Version.Pattern)
 		}
