diff --git a/.github/workflows/expedite.yml b/.github/workflows/expedite.yml
index 84d4a22..cbac8a0 100644
--- a/.github/workflows/expedite.yml
+++ b/.github/workflows/expedite.yml
@@ -1,14 +1,15 @@
 name: "Expedite"
 
-# Expediting: image update automation for the game server.
+# Expediting: keeping the game server on the build Valve publishes.
 #
 # Expediting is one duty of procurement (scripts/procurement), and the only one
 # that holds elevated permission. On a construction site, expediting chases an
 # order with a supplier and gets material to site when it is needed, without
 # building or installing anything. This does the same for one item from one
 # approved supplier: when Valve
-# publishes a new build of the Valheim dedicated server, it takes delivery -
-# builds the image, pins the digest - and submits it.
+# publishes a new build of the Valheim dedicated server, it records the build
+# in scripts/versions.env and submits that one line. The fabricator builds the
+# image and the release from it.
 #
 # WHY IT EXISTS. Valheim clients update themselves through Steam and refuse a
 # server on an older build, so every game patch strands every player until the
@@ -20,10 +21,11 @@ name: "Expedite"
 # THREE JOBS, BECAUSE THEY COST WILDLY DIFFERENT AMOUNTS.
 #
 #   check    one HTTPS call to Steam's news API. Has Valve posted anything?
-#   deliver  the expensive half, and only when check says to look: SteamCMD for
-#            the real build id, then a 2 GB download, a build, a push, and a
-#            pull request changing the pinned digest and the recorded build
-#   release  at 4am local, merge what is waiting
+#   deliver  only when check says to look: SteamCMD for the real build id,
+#            and a pull request changing VALHEIM_STEAM_BUILD_VERSION
+#   release  at 4am local: merge that pull request, wait for the fabricator
+#            to deliver the release built from it, and merge the delivery if
+#            the Steam build is all that changed
 #
 # The first version of this ran the whole check every fifteen minutes, which
 # meant installing a 32-bit library and SteamCMD ninety-six times a day to ask
@@ -39,22 +41,24 @@ name: "Expedite"
 # estate believes it is current: the daily pass below takes the authoritative
 # route whatever the doorbell said.
 #
-# WHAT A MERGE DOES NOW. Production no longer follows main or a tag: it runs
-# the release pinned in clusters/management/releases.yaml (#510). Merging a
-# delivery changes what the fabricator builds into the next release, and a
-# release reaches production only when a pull request moves that pin. The
-# 4am window still governs the merge, so an unattended delivery lands when it
-# disturbs nobody; moving the pin automatically, under a narrower standing
-# order, is the next part of #510.
+# WHAT A MERGE DOES NOW. Production runs the release pinned in
+# clusters/management/releases.yaml (#510). Merging the expediter's pull
+# request changes what the fabricator builds; the fabricator publishes a
+# release and procurement delivers it as a pull request moving that pin. The
+# 4am window takes both - the second only when its release differs from
+# production's by the Steam build alone - so a Steam patch reaches production
+# in one window with nobody pressing anything, and anything a person wrote
+# still waits for a person.
 #
 # WHAT KEEPS IT HONEST. The standing order is the procurement GitHub App
 # being a bypass actor on the "protected branches" ruleset. That ruleset holds
 # the review rule and the required checks together, so the bypass skips both:
 # a required check cannot bound it. The release job therefore takes only pull
 # requests the App itself opened, runs `superintendent enforce-standing-order`
-# against the exact head commit it is about to merge - refusing anything but
-# the digest and the recorded build, or a digest from another image
-# repository - and merges that commit and no later one. The same check runs in
+# against the exact head commit it is about to merge - refusing an expediter
+# change beyond the Steam build line, and a delivery whose release differs
+# from production's by more than that build, judged from the commits both
+# releases were built from - and merges that commit and no later one. The same check runs in
 # Sensitive Paths so the verdict shows on the pull request, but it is the one
 # here that binds.
 #
@@ -131,10 +135,11 @@ defaults:
     shell: bash
 
 env:
-  PIN: modules/applications/valheim/base/deployment.yaml
+  # The one line the expediter moves. The fabricator builds the image and the
+  # release from it; the expediter builds nothing.
+  PIN: scripts/versions.env
   APP_ID: "896660"
   REPO: ${{ github.repository }}
-  IMAGE: ghcr.io/${{ github.repository }}-valheim
 
 jobs:
   check:
@@ -203,13 +208,11 @@ jobs:
     needs: check
     if: needs.check.outputs.look == 'true'
     runs-on: ubuntu-latest
-    # Bounded so a stuck run cannot bill forever. The longest honest path is a
-    # 2 GB download, a build and a push; waiting for checks belongs to the
-    # release job, so this no longer has to cover that too.
-    timeout-minutes: 45
+    # Bounded so a stuck run cannot bill forever. It asks SteamCMD one
+    # question and commits one line; the image is the fabricator's to build.
+    timeout-minutes: 20
     permissions:
       contents: read
-      packages: write
     steps:
       # Audit, not block, and this is why: SteamCMD reaches Valve's content
       # servers, which are a changing set of hosts - approved-suppliers.yml
@@ -226,12 +229,6 @@ jobs:
           egress-policy: audit
           disable-sudo-and-containers: "false"
 
-      # The image takes rclone at the version scripts/versions.env pins, as a
-      # build argument - the fabricator passes it automatically, and this
-      # build has to pass it too until expediting stops building (#510).
-      - name: Read pinned versions
-        uses: ./.github/actions/versions
-
       - name: Ask SteamCMD what the build actually is
         id: build
         run: |
@@ -286,8 +283,8 @@ jobs:
             exit 1
           fi
 
-          recorded="$(sed -n 's/^[[:space:]]*[a-z0-9.-]*\/steam-build:[[:space:]]*"\([^"]*\)".*/\1/p' "$PIN" | head -n 1)"
-          echo "Steam's public build: ${build}; recorded beside the pin: ${recorded:-nothing}"
+          recorded="$(sed -n 's/^VALHEIM_STEAM_BUILD_VERSION=\([0-9]*\)$/\1/p' "$PIN" | head -n 1)"
+          echo "Steam's public build: ${build}; pinned in ${PIN}: ${recorded:-nothing}"
 
           if [ "$build" = "$recorded" ]; then
             echo "no delivery: the server already runs build ${build}" >> "$GITHUB_STEP_SUMMARY"
@@ -301,38 +298,6 @@ jobs:
             echo "recorded=${recorded}"
           } >> "$GITHUB_OUTPUT"
 
-      - name: Build the image for exactly that build
-        id: image
-        if: steps.build.outputs.deliver == 'true'
-        env:
-          TOKEN: ${{ secrets.GITHUB_TOKEN }}
-          ACTOR: ${{ github.actor }}
-          BUILD: ${{ steps.build.outputs.build }}
-        run: |
-          set -euo pipefail
-          echo "$TOKEN" | docker login ghcr.io -u "$ACTOR" --password-stdin
-
-          # The build argument makes the image refuse to build if Steam serves
-          # anything but this build, so the label - and the record in git - is
-          # true of what is inside.
-          docker build \
-            --build-arg VALHEIM_BUILDID="$BUILD" \
-            --build-arg RCLONE_DEB_VERSION="$RCLONE_DEB_VERSION" \
-            -t "${IMAGE}:steam-${BUILD}" \
-            modules/applications/valheim/image
-          docker push "${IMAGE}:steam-${BUILD}"
-
-          # The manifest digest from the registry, not the local image id -
-          # the two sit side by side in a build log and are not the same thing.
-          digest="$(docker inspect --format='{{index .RepoDigests 0}}' "${IMAGE}:steam-${BUILD}")"
-          digest="${digest#*@}"
-          case "$digest" in
-            sha256:*) ;;
-            *) echo "::error::the registry gave no manifest digest for ${IMAGE}:steam-${BUILD}"; exit 1 ;;
-          esac
-          echo "digest=${digest}" >> "$GITHUB_OUTPUT"
-          echo "built ${IMAGE}@${digest}" >> "$GITHUB_STEP_SUMMARY"
-
       - name: Mint the expedite token
         id: token
         if: steps.build.outputs.deliver == 'true'
@@ -361,7 +326,6 @@ jobs:
           GH_TOKEN: ${{ steps.token.outputs.token }}
           BUILD: ${{ steps.build.outputs.build }}
           RECORDED: ${{ steps.build.outputs.recorded }}
-          DIGEST: ${{ steps.image.outputs.digest }}
           RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
         run: |
           set -euo pipefail
@@ -381,10 +345,9 @@ jobs:
           base_tree="$(gh api "repos/${REPO}/git/commits/${base}" --jq .tree.sha)"
           gh api "repos/${REPO}/contents/${PIN}?ref=${base}" --jq .content | base64 -d > "$RUNNER_TEMP/pin.yaml"
 
-          sed -i -E "s#(image:[[:space:]]+[^@[:space:]]+@)sha256:[0-9a-f]{64}#\1${DIGEST}#" "$RUNNER_TEMP/pin.yaml"
-          sed -i -E "s#([a-z0-9.-]+/steam-build:[[:space:]]+)\"[^\"]*\"#\1\"${BUILD}\"#" "$RUNNER_TEMP/pin.yaml"
-          if ! grep -q "@${DIGEST}" "$RUNNER_TEMP/pin.yaml" || ! grep -q "steam-build: \"${BUILD}\"" "$RUNNER_TEMP/pin.yaml"; then
-            echo "::error::could not find the pinned image and the steam-build annotation in ${PIN}; its shape has changed and this needs updating"
+          sed -i -E "s#^VALHEIM_STEAM_BUILD_VERSION=[0-9]*\$#VALHEIM_STEAM_BUILD_VERSION=${BUILD}#" "$RUNNER_TEMP/pin.yaml"
+          if ! grep -qx "VALHEIM_STEAM_BUILD_VERSION=${BUILD}" "$RUNNER_TEMP/pin.yaml"; then
+            echo "::error::could not find the VALHEIM_STEAM_BUILD_VERSION line in ${PIN}; its shape has changed and this needs updating"
             exit 1
           fi
 
@@ -400,10 +363,8 @@ jobs:
             "feat: take delivery of Valheim server build ${BUILD}" \
             "" \
             "Valve published a new build of the dedicated server, and clients refuse" \
-            "a server on an older one. Pins the image built from it." \
-            "" \
-            "Image:  ${IMAGE}" \
-            "Digest: ${DIGEST}" \
+            "a server on an older one. Pins it; the fabricator builds the image and" \
+            "the release from the pin." \
             "" \
             "Taken by expediting: ${RUN_URL}")"
           # No author, committer or signature field: GitHub signs on the App's
@@ -416,18 +377,11 @@ jobs:
             --body "$(printf '%s\n' \
               "## What this is" \
               "" \
-              "Valve published build **${BUILD}** of the Valheim dedicated server. Players' clients update themselves and refuse a server on an older build, so this pins the image built from it." \
-              "" \
-              "| | |" \
-              "| --- | --- |" \
-              "| Steam build | \`${RECORDED:-unknown}\` → \`${BUILD}\` |" \
-              "| Image | \`${IMAGE}@${DIGEST}\` |" \
+              "Valve published build **${BUILD}** of the Valheim dedicated server. Players' clients update themselves and refuse a server on an older build, so this pins it in \`${PIN}\`: Steam build \`${RECORDED:-unknown}\` → \`${BUILD}\`." \
               "" \
               "## What happens next" \
               "" \
-              "Every required check runs, including \`superintendent enforce-standing-order\`, which refuses this pull request if it changes anything but the digest and the recorded build." \
-              "" \
-              "**Merging this does not release it.** It changes what the fabricator builds into the next release; production moves when a pull request changes the pin in \`clusters/management/releases.yaml\`. Leave it and it merges at 4am America/Chicago. Nothing here waits for an approval." \
+              "Nothing needs you. At 4am America/Chicago the expediter merges this under its standing order, which covers this one line and refuses anything more. The fabricator then builds the image and the release, procurement delivers it, and the same window merges that delivery if the Steam build is all that changed. A release carrying anything else waits for a review." \
               "" \
               "Taken by expediting: ${RUN_URL}")")"
           echo "opened ${url##*/}" >> "$GITHUB_STEP_SUMMARY"
@@ -444,7 +398,13 @@ jobs:
        github.event.schedule == '0 9 * * *' ||
        github.event.schedule == '0 10 * * *')
     runs-on: ubuntu-latest
-    timeout-minutes: 40
+    # Long enough to merge the expediter's pull request, wait for the
+    # fabricator to build and deliver the release, and take that too.
+    timeout-minutes: 100
+    # Actions read, to find and follow the fabricator's run on the merge.
+    permissions:
+      contents: read
+      actions: read
     steps:
       - name: Mobilize
         uses: $/.github/workflows/mobilize
@@ -457,6 +417,7 @@ jobs:
           disable-sudo-and-containers: "true"
           allowed-endpoints: >
             api.github.com:443
+            ghcr.io:443
             github.com:443
             go.dev:443
             objects.githubusercontent.com:443
@@ -582,7 +543,7 @@ jobs:
           # -bypass: this merge skips review, so only what the order covers
           # passes - a delivery of a release never does.
           if ! go run -C scripts/superintendent . enforce-standing-order -bypass -author "$author" -base "$base" -head "$head"; then
-            gh pr comment "$PR" --repo "$REPO" --body "Refused under the standing order at merge time: this changes more than the image digest and Steam build. It waits for a review."
+            gh pr comment "$PR" --repo "$REPO" --body "Refused under the standing order at merge time: this changes more than the Steam build. It waits for a review."
             exit 1
           fi
           build="$(gh pr view "$PR" --repo "$REPO" --json headRefName --jq '.headRefName | sub("^expedite/valheim-steam-"; "")')"
@@ -601,3 +562,80 @@ jobs:
             exit 1
           fi
           echo "merged #${PR}: Steam build ${build}" >> "$GITHUB_STEP_SUMMARY"
+          echo "sha=$(gh pr view "$PR" --repo "$REPO" --json mergeCommit --jq .mergeCommit.oid)" >> "$GITHUB_OUTPUT"
+
+      # The pull request just merged only changes what the fabricator builds.
+      # Production moves when the release built from it is delivered, so the
+      # window waits for that delivery rather than leaving it for tomorrow's:
+      # players' clients update the day Valve ships and refuse an older server.
+      - name: Wait for the fabricator to deliver the release
+        if: steps.merge.outputs.sha != ''
+        env:
+          GH_TOKEN: ${{ github.token }}
+          SHA: ${{ steps.merge.outputs.sha }}
+        run: |
+          set -euo pipefail
+          run=""
+          for _ in $(seq 1 20); do
+            run="$(gh run list --repo "$REPO" --workflow fabricator.yml --commit "$SHA" --json databaseId --jq '.[0].databaseId // empty')"
+            [ -n "$run" ] && break
+            sleep 15
+          done
+          if [ -z "$run" ]; then
+            echo "::error::the fabricator never started for ${SHA}, so there is no release to take"
+            exit 1
+          fi
+          if ! timeout 60m gh run watch "$run" --repo "$REPO" --exit-status --interval 30 >/dev/null; then
+            echo "::error::the fabricator's run ${run} did not succeed, so its release is left for a person"
+            exit 1
+          fi
+          echo "the fabricator finished run ${run}" >> "$GITHUB_STEP_SUMMARY"
+
+      # Every delivery the holder opened is taken if the release changes
+      # nothing but the Steam build - judged against the commits its releases
+      # were built from, which the registry records. Anything else waits for a
+      # review, however it arrived.
+      - name: Release the deliveries whose only change is the Steam build
+        if: steps.window.outputs.due == 'true' && !failure()
+        env:
+          GH_TOKEN: ${{ steps.token.outputs.token }}
+          HOLDER: ${{ vars.PROCUREMENT_BOT_LOGIN }}
+        run: |
+          set -euo pipefail
+          if [ -z "$HOLDER" ]; then
+            echo "no standing order is configured, so no delivery is released" >> "$GITHUB_STEP_SUMMARY"
+            exit 0
+          fi
+          prs="$(gh api "repos/${REPO}/pulls?state=open&per_page=100" \
+            --jq '[.[] | select(.user.login == env.HOLDER and .head.repo.full_name == env.REPO and (.head.ref | startswith("deliver/")))] | sort_by(.number) | .[].number')"
+          if [ -z "$prs" ]; then
+            echo "no delivery is waiting" >> "$GITHUB_STEP_SUMMARY"
+            exit 0
+          fi
+          for pr in $prs; do
+            for _ in $(seq 1 20); do
+              rc=0
+              gh pr checks "$pr" --repo "$REPO" --required >/dev/null 2>&1 || rc=$?
+              if [ "$rc" -eq 0 ] || [ "$rc" -eq 8 ]; then break; fi
+              sleep 15
+            done
+            if ! timeout 20m gh pr checks "$pr" --repo "$REPO" --required --watch --interval 30 --fail-fast >/dev/null; then
+              echo "#${pr}: a required check did not pass, so it is left for a person" >> "$GITHUB_STEP_SUMMARY"
+              continue
+            fi
+            read -r author head target <<<"$(gh api "repos/${REPO}/pulls/${pr}" --jq '"\(.user.login) \(.head.sha) \(.base.ref)"')"
+            base="$(gh api "repos/${REPO}/compare/${target}...${head}" --jq .merge_base_commit.sha)"
+            git fetch --no-tags --depth=1 origin "$base" "$head"
+            if ! go run -C scripts/superintendent . enforce-standing-order -bypass -author "$author" -base "$base" -head "$head"; then
+              echo "#${pr}: the release changes more than the Steam build, so it waits for a review" >> "$GITHUB_STEP_SUMMARY"
+              continue
+            fi
+            title="$(gh pr view "$pr" --repo "$REPO" --json title --jq .title)"
+            if ! gh pr merge "$pr" --repo "$REPO" --squash --admin --match-head-commit "$head" \
+              --subject "${title} (#${pr})" \
+              --body "Merged under the standing order in the 4am window: the release changes nothing but the Steam build."; then
+              gh pr comment "$pr" --repo "$REPO" --body "The release changes nothing but the Steam build and every required check passed, but GitHub refused the merge. It waits for a review."
+              continue
+            fi
+            echo "merged #${pr}: ${title}" >> "$GITHUB_STEP_SUMMARY"
+          done
diff --git a/.github/workflows/fabricator.yml b/.github/workflows/fabricator.yml
index 042ff78..026f977 100644
--- a/.github/workflows/fabricator.yml
+++ b/.github/workflows/fabricator.yml
@@ -138,6 +138,47 @@ jobs:
           done < <(sed -n 's/^ARG[[:space:]]\{1,\}\([A-Za-z_][A-Za-z0-9_]*\)[[:space:]]*$/\1/p' "${CONTEXT}/Dockerfile" | sort -u)
           echo "args=${args# }" >> "$GITHUB_OUTPUT"
 
+      # Whether this exact image already exists (#514). The fingerprint is
+      # every tracked file in the build context and every pin passed to it -
+      # the whole of what the image is made from - and a build of the same
+      # fingerprint is reused rather than repeated. Rebuilding is not free
+      # here even when it succeeds: a game server's build downloads the game
+      # again and is never byte-identical, so every rebuild used to become a
+      # new digest, a new release and a new counter value for nothing.
+      - name: Reuse a build of the same inputs
+        id: reuse
+        env:
+          GH_TOKEN: ${{ github.token }}
+          OWNER: ${{ github.repository_owner }}
+          PACKAGE: ${{ github.event.repository.name }}-${{ matrix.order.name }}
+          CONTEXT: ${{ matrix.order.context }}
+          PINS: ${{ steps.pins.outputs.args }}
+        run: |
+          fingerprint="fp-$( { git ls-tree -r HEAD -- "$CONTEXT"; printf '%s\n' "$PINS"; } | sha256sum | cut -c1-16)"
+          echo "fingerprint=${fingerprint}" >> "$GITHUB_OUTPUT"
+          case "$(gh api "users/${OWNER}" --jq .type)" in
+            Organization) base="orgs/${OWNER}" ;;
+            User) base="users/${OWNER}" ;;
+            *)
+              echo "::error::could not tell whether ${OWNER} is an organization or a user"
+              exit 1
+              ;;
+          esac
+          if ! listed="$(gh api --paginate "${base}/packages/container/${PACKAGE}/versions" \
+            --jq ".[] | select(.metadata.container.tags | index(\"${fingerprint}\")) | .name" 2>&1)"; then
+            if grep -q "HTTP 404" <<<"$listed"; then
+              listed=""
+            else
+              echo "::error::could not list the builds of ${PACKAGE}: ${listed}"
+              exit 1
+            fi
+          fi
+          digest="${listed%%$'\n'*}"
+          if [ -n "$digest" ]; then
+            echo "digest=${digest}" >> "$GITHUB_OUTPUT"
+            echo "reusing ${digest}: an image built from these exact inputs exists" >> "$GITHUB_STEP_SUMMARY"
+          fi
+
       - name: Log in to the registry
         env:
           TOKEN: ${{ secrets.GITHUB_TOKEN }}
@@ -155,16 +196,24 @@ jobs:
           PINS: ${{ steps.pins.outputs.args }}
           SHA: ${{ github.sha }}
           SOURCE: ${{ github.server_url }}/${{ github.repository }}
+          FINGERPRINT: ${{ steps.reuse.outputs.fingerprint }}
+          REUSED: ${{ steps.reuse.outputs.digest }}
         run: |
+          if [ -n "$REUSED" ]; then
+            echo "digest=${REUSED}" >> "$GITHUB_OUTPUT"
+            exit 0
+          fi
           # shellcheck disable=SC2086 # PINS is a list of flags by construction
           docker build $PINS \
             --label "org.opencontainers.image.revision=${SHA}" \
             --label "org.opencontainers.image.source=${SOURCE}" \
             -t "${IMAGE}:${SHA}" \
             -t "${IMAGE}:latest" \
+            -t "${IMAGE}:${FINGERPRINT}" \
             "$CONTEXT"
           docker push "${IMAGE}:${SHA}"
           docker push "${IMAGE}:latest"
+          docker push "${IMAGE}:${FINGERPRINT}"
           digest="$(docker inspect --format='{{index .RepoDigests 0}}' "${IMAGE}:${SHA}")"
           {
             echo "### ${IMAGE##*/}"
@@ -186,8 +235,59 @@ jobs:
       # image, what the version line looks like - comes from the order.
       # tests/go/repo/fabricator_test.go runs each of these steps as shipped.
 
-      - name: Take delivery of the Flux CLI
+      # Whether this exact release already exists (#514). Fingerprinted from
+      # the manifests it would ship, rendered, with the image pinned: what
+      # reaches the cluster, and nothing else. A comment in the overlay renders
+      # to nothing, so it makes no release - which is what it did before,
+      # delivering a "new" release identical to the one running.
+      - name: Reuse a release of the same manifests
+        id: release_fp
         if: matrix.order.release
+        env:
+          GH_TOKEN: ${{ github.token }}
+          OWNER: ${{ github.repository_owner }}
+          PACKAGE: ${{ github.event.repository.name }}-${{ matrix.order.name }}-release
+          IMAGE: ghcr.io/${{ github.repository }}-${{ matrix.order.name }}
+          DIGEST: ${{ steps.build.outputs.digest }}
+          RELEASE: ${{ toJSON(matrix.order.release) }}
+        run: |
+          overlay="$(jq -r '.overlay' <<<"$RELEASE")"
+          module="$(jq -r '.module' <<<"$RELEASE")"
+          rendered="${RUNNER_TEMP}/fingerprint"
+          rm -rf "$rendered"
+          for dir in "$module" "$overlay"; do
+            mkdir -p "${rendered}/$(dirname "$dir")"
+            cp -R "$dir" "${rendered}/${dir}"
+          done
+          printf '%s\n' "images:" "  - name: ${IMAGE}" "    digest: ${DIGEST}" >> "${rendered}/${overlay}/kustomization.yaml"
+          fingerprint="fp-$(kubectl kustomize "${rendered}/${overlay}" | sha256sum | cut -c1-16)"
+          echo "fingerprint=${fingerprint}" >> "$GITHUB_OUTPUT"
+          case "$(gh api "users/${OWNER}" --jq .type)" in
+            Organization) base="orgs/${OWNER}" ;;
+            User) base="users/${OWNER}" ;;
+            *)
+              echo "::error::could not tell whether ${OWNER} is an organization or a user"
+              exit 1
+              ;;
+          esac
+          if ! listed="$(gh api --paginate "${base}/packages/container/${PACKAGE}/versions" \
+            --jq ".[] | select(.metadata.container.tags | index(\"${fingerprint}\")) | .metadata.container.tags[]" 2>&1)"; then
+            if grep -q "HTTP 404" <<<"$listed"; then
+              listed=""
+            else
+              echo "::error::could not list the releases of ${PACKAGE}: ${listed}"
+              exit 1
+            fi
+          fi
+          if [ -n "$listed" ]; then
+            echo "needed=false" >> "$GITHUB_OUTPUT"
+            echo "no release: $(grep -v '^fp-' <<<"$listed" | head -n 1) already ships these exact manifests" >> "$GITHUB_STEP_SUMMARY"
+          else
+            echo "needed=true" >> "$GITHUB_OUTPUT"
+          fi
+
+      - name: Take delivery of the Flux CLI
+        if: matrix.order.release && steps.release_fp.outputs.needed == 'true'
         run: scripts/take-delivery.sh flux
 
       # Started once, with the environment the order gives it, and read from
@@ -196,7 +296,7 @@ jobs:
       # release.
       - name: Read the version the build carries
         id: version
-        if: matrix.order.release
+        if: matrix.order.release && steps.release_fp.outputs.needed == 'true'
         env:
           IMAGE: ghcr.io/${{ github.repository }}-${{ matrix.order.name }}
           DIGEST: ${{ steps.build.outputs.digest }}
@@ -229,7 +329,7 @@ jobs:
       # holds. None yet - the package does not exist - is the first release.
       - name: Number the release
         id: number
-        if: matrix.order.release
+        if: matrix.order.release && steps.release_fp.outputs.needed == 'true'
         env:
           GH_TOKEN: ${{ github.token }}
           OWNER: ${{ github.repository_owner }}
@@ -266,7 +366,7 @@ jobs:
 
       - name: Stage and publish the release
         id: release
-        if: matrix.order.release
+        if: matrix.order.release && steps.release_fp.outputs.needed == 'true'
         env:
           IMAGE: ghcr.io/${{ github.repository }}-${{ matrix.order.name }}
           DIGEST: ${{ steps.build.outputs.digest }}
@@ -275,6 +375,7 @@ jobs:
           RELEASE: ${{ toJSON(matrix.order.release) }}
           SHA: ${{ github.sha }}
           SOURCE: ${{ github.server_url }}/${{ github.repository }}
+          FINGERPRINT: ${{ steps.release_fp.outputs.fingerprint }}
         run: |
           overlay="$(jq -r '.overlay' <<<"$RELEASE")"
           module="$(jq -r '.module' <<<"$RELEASE")"
@@ -295,6 +396,7 @@ jobs:
             --reproducible \
             --output=json > "${RUNNER_TEMP}/pushed.json"
           pushed="$(jq -r '.digest' "${RUNNER_TEMP}/pushed.json")"
+          flux tag artifact "oci://${ARTIFACT}:${TAG}" --tag "${FINGERPRINT}"
           echo "digest=${pushed}" >> "$GITHUB_OUTPUT"
           {
             echo ""
@@ -389,7 +491,7 @@ jobs:
           while IFS= read -r name; do
             package="${REPO_NAME}-${name}-release"
             read -r tag digest < <(GH_TOKEN="$READ_TOKEN" gh api --paginate "${base_path}/packages/container/${package}/versions" \
-              --jq '.[] | select(.metadata.container.tags | length > 0) | "\(.created_at) \(.metadata.container.tags[0]) \(.name)"' \
+              --jq '.[] | (.metadata.container.tags | map(select(startswith("fp-") | not)) | .[0]) as $tag | select($tag != null) | "\(.created_at) \($tag) \(.name)"' \
               | sort -r | head -n 1 | cut -d' ' -f2-)
             if [ -z "${tag:-}" ]; then
               echo "::error::${package} holds no release, although the ${name} order makes one"
diff --git a/scripts/approved-suppliers.yml b/scripts/approved-suppliers.yml
index 3717f0e..dbc41e5 100644
--- a/scripts/approved-suppliers.yml
+++ b/scripts/approved-suppliers.yml
@@ -691,13 +691,15 @@ egress:
       doorbell above and the release below are both blocked to a list.
 
     root: >-
-      Installs lib32gcc-s1 with apt-get, which SteamCMD needs, and builds the
-      image with Docker. Both need root or the Docker socket, so
-      disable-sudo-and-containers would break the job it exists for.
+      Installs lib32gcc-s1 with apt-get, which SteamCMD needs to ask for the
+      build id. That needs root, so disable-sudo-and-containers would break
+      the job it exists for. It no longer builds anything: the fabricator
+      builds the image from the pin this job records.
   - job: expedite.yml/release
     policy: block
     hosts:
       - api.github.com:443
+      - ghcr.io:443
       - github.com:443
       - go.dev:443
       - objects.githubusercontent.com:443
diff --git a/tests/go/repo/fabricator_test.go b/tests/go/repo/fabricator_test.go
index 9296188..1989835 100644
--- a/tests/go/repo/fabricator_test.go
+++ b/tests/go/repo/fabricator_test.go
@@ -375,7 +375,8 @@ echo Organization`})
 // by digest in the overlay - and that is what is pushed.
 func TestTheReleaseCarriesTheOverlayWithTheNewDigest(t *testing.T) {
 	scratch := t.TempDir()
-	path := fakeTools(t, map[string]string{"flux": `path=""
+	path := fakeTools(t, map[string]string{"flux": `if [ "$1" = tag ]; then echo "$@" > "` + scratch + `/tagged"; exit 0; fi
+path=""
 for a in "$@"; do case "$a" in --path=*) path="${a#--path=}" ;; esac; done
 cp -R "$path" "` + scratch + `/pushed"
 echo '{"repository":"ghcr.io/example/homelab-valheim-release","tag":"1.0.15-1","digest":"sha256:` + strings.Repeat("b", 64) + `"}'`})
@@ -383,10 +384,16 @@ echo '{"repository":"ghcr.io/example/homelab-valheim-release","tag":"1.0.15-1","
 	ok, output, logs := runReleaseStep(t, "Stage and publish the release", repoRoot(t), path, []string{
 		"IMAGE=ghcr.io/example/homelab-valheim", "DIGEST=" + digest,
 		"ARTIFACT=ghcr.io/example/homelab-valheim-release", "TAG=1.0.15-1",
-		"RELEASE=" + valheimRelease(t), "SHA=0123456789abcdef", "SOURCE=https://github.com/example/homelab"})
+		"RELEASE=" + valheimRelease(t), "SHA=0123456789abcdef", "SOURCE=https://github.com/example/homelab",
+		"FINGERPRINT=fp-0123456789abcdef"})
 	if !ok {
 		t.Fatalf("the release step failed:\n%s", logs)
 	}
+	tagged, _ := os.ReadFile(filepath.Join(scratch, "tagged"))
+	if !strings.Contains(string(tagged), "1.0.15-1") || !strings.Contains(string(tagged), "--tag fp-0123456789abcdef") {
+		t.Errorf("the published release was not tagged with its fingerprint, so the next build of the "+
+			"same manifests would publish it again (#514): %q", tagged)
+	}
 	if !strings.Contains(output, "digest=sha256:"+strings.Repeat("b", 64)) {
 		t.Errorf("the step did not report the pushed artifact's digest: %q", output)
 	}
@@ -430,3 +437,164 @@ func TestEveryReleaseOrderIsOneTheFabricatorCanFollow(t *testing.T) {
 		t.Fatal("no work order asks for a release, so production has nothing to pin")
 	}
 }
+
+// --- reusing what already exists (#514) --------------------------------------
+
+// A registry fake for the reuse steps: the owner lookup, and a package listing
+// whose answer the test chooses. gh evaluates its own --jq, so the fake answers
+// with what that filter would have selected.
+func fakeRegistryListing(t *testing.T, listing string) string {
+	t.Helper()
+	return fakeTools(t, map[string]string{"gh": `if [ "$2" = "--paginate" ]; then set -- "$1" "$3"; fi
+case "$2" in
+  users/*) echo Organization ;;
+  *) ` + listing + ` ;;
+esac`})
+}
+
+// The image fingerprint is the build context's tracked files and the pins
+// passed to it: the same inputs give the same fingerprint, and a change to
+// either gives a new one. A build of that fingerprint already in the registry
+// is reused rather than repeated.
+func TestAnImageOfTheSameInputsIsReused(t *testing.T) {
+	repo := t.TempDir()
+	run := func(args ...string) {
+		t.Helper()
+		cmd := exec.Command("git", append([]string{"-C", repo, "-c", "user.email=a@example.com", "-c", "user.name=t"}, args...)...)
+		cmd.Env = append(os.Environ(), "GIT_CONFIG_GLOBAL=/dev/null", "GIT_CONFIG_SYSTEM=/dev/null")
+		if out, err := cmd.CombinedOutput(); err != nil {
+			t.Fatalf("git %v: %v\n%s", args, err, out)
+		}
+	}
+	run("init", "-q")
+	if err := os.MkdirAll(filepath.Join(repo, "img"), 0o755); err != nil {
+		t.Fatal(err)
+	}
+	write := func(body string) {
+		if err := os.WriteFile(filepath.Join(repo, "img", "Dockerfile"), []byte(body), 0o644); err != nil {
+			t.Fatal(err)
+		}
+		run("add", "-A")
+		run("commit", "-qm", "img")
+	}
+	fingerprint := func(pins, listing string) (map[string]string, string) {
+		ok, output, logs := runReleaseStepIn(t, "Reuse a build of the same inputs", repo, fakeRegistryListing(t, listing),
+			[]string{"OWNER=example", "PACKAGE=homelab-valheim", "CONTEXT=img", "PINS=" + pins, "GH_TOKEN=x"})
+		if !ok {
+			t.Fatalf("the reuse step failed:\n%s", logs)
+		}
+		out := map[string]string{}
+		for _, l := range strings.Split(strings.TrimSpace(output), "\n") {
+			if k, v, ok := strings.Cut(l, "="); ok {
+				out[k] = v
+			}
+		}
+		return out, logs
+	}
+
+	write("ARG VALHEIM_STEAM_BUILD_VERSION\nFROM scratch\n")
+	first, _ := fingerprint("--build-arg VALHEIM_STEAM_BUILD_VERSION=1", `echo "gh: Not Found (HTTP 404)" >&2; exit 1`)
+	again, _ := fingerprint("--build-arg VALHEIM_STEAM_BUILD_VERSION=1", `echo "gh: Not Found (HTTP 404)" >&2; exit 1`)
+	if first["fingerprint"] == "" || first["fingerprint"] != again["fingerprint"] {
+		t.Fatalf("the same inputs fingerprinted differently: %v then %v", first, again)
+	}
+	if first["digest"] != "" {
+		t.Errorf("an image was reused when the registry holds no build at all: %v", first)
+	}
+	newPin, _ := fingerprint("--build-arg VALHEIM_STEAM_BUILD_VERSION=2", `echo "gh: Not Found (HTTP 404)" >&2; exit 1`)
+	if newPin["fingerprint"] == first["fingerprint"] {
+		t.Error("a new Steam build kept the old fingerprint, so its image would never be built")
+	}
+	write("ARG VALHEIM_STEAM_BUILD_VERSION\nFROM scratch\nUSER 1\n")
+	newFile, _ := fingerprint("--build-arg VALHEIM_STEAM_BUILD_VERSION=1", `echo "gh: Not Found (HTTP 404)" >&2; exit 1`)
+	if newFile["fingerprint"] == first["fingerprint"] {
+		t.Error("a change to the build context kept the old fingerprint, so its image would never be built")
+	}
+
+	digest := "sha256:" + strings.Repeat("c", 64)
+	reused, _ := fingerprint("--build-arg VALHEIM_STEAM_BUILD_VERSION=1", "echo "+digest)
+	if reused["digest"] != digest {
+		t.Errorf("a build of these inputs exists and was not reused: %v", reused)
+	}
+
+	ok, _, logs := runReleaseStepIn(t, "Reuse a build of the same inputs", repo,
+		fakeRegistryListing(t, `echo "gh: Server Error (HTTP 500)" >&2; exit 1`),
+		[]string{"OWNER=example", "PACKAGE=homelab-valheim", "CONTEXT=img", "PINS=x", "GH_TOKEN=x"})
+	if ok {
+		t.Errorf("a registry that could not be read was taken to hold no build:\n%s", logs)
+	}
+}
+
+// A release is fingerprinted from the manifests it would ship, rendered with
+// the image pinned. A comment renders to nothing, so it makes no new release -
+// the #514 case, where a comment in the overlay was delivered as a release
+// identical to the one running. A new image digest is a new release.
+func TestAReleaseOfTheSameManifestsIsNotRepeated(t *testing.T) {
+	stage := t.TempDir()
+	for _, dir := range []string{"environments/production/applications/valheim", "modules/applications/valheim"} {
+		if err := exec.Command("mkdir", "-p", filepath.Join(stage, filepath.Dir(dir))).Run(); err != nil {
+			t.Fatal(err)
+		}
+		if out, err := exec.Command("cp", "-R", filepath.Join(repoRoot(t), dir), filepath.Join(stage, dir)).CombinedOutput(); err != nil {
+			t.Fatalf("copying %s: %v\n%s", dir, err, out)
+		}
+	}
+	// The image name the module's own manifest uses, which is what the
+	// workflow's IMAGE resolves to: kustomize replaces an image only by name.
+	image := ""
+	for _, l := range strings.Split(readRepoFile(t, "modules/applications/valheim/base/deployment.yaml"), "\n") {
+		if name, _, ok := strings.Cut(strings.TrimSpace(strings.TrimPrefix(strings.TrimSpace(l), "image:")), "@"); ok && strings.HasPrefix(strings.TrimSpace(l), "image:") {
+			image = strings.TrimSpace(name)
+			break
+		}
+	}
+	if image == "" {
+		t.Fatal("the module names no image by digest, so the release has nothing to pin")
+	}
+	fingerprint := func(digest, listing string) map[string]string {
+		ok, output, logs := runReleaseStepIn(t, "Reuse a release of the same manifests", stage, fakeRegistryListing(t, listing),
+			[]string{"OWNER=example", "PACKAGE=homelab-valheim-release", "IMAGE=" + image,
+				"DIGEST=" + digest, "RELEASE=" + valheimRelease(t), "GH_TOKEN=x"})
+		if !ok {
+			t.Fatalf("the release fingerprint step failed:\n%s", logs)
+		}
+		out := map[string]string{}
+		for _, l := range strings.Split(strings.TrimSpace(output), "\n") {
+			if k, v, ok := strings.Cut(l, "="); ok {
+				out[k] = v
+			}
+		}
+		return out
+	}
+	none := `echo "gh: Not Found (HTTP 404)" >&2; exit 1`
+	a := "sha256:" + strings.Repeat("a", 64)
+
+	first := fingerprint(a, none)
+	if first["needed"] != "true" {
+		t.Fatalf("a release nobody has published was not needed: %v", first)
+	}
+
+	kustomization := filepath.Join(stage, "environments/production/applications/valheim/kustomization.yaml")
+	body, err := os.ReadFile(kustomization)
+	if err != nil {
+		t.Fatal(err)
+	}
+	if err := os.WriteFile(kustomization, append([]byte("# a comment reaches no cluster\n"), body...), 0o644); err != nil {
+		t.Fatal(err)
+	}
+	commented := fingerprint(a, none)
+	if commented["fingerprint"] != first["fingerprint"] {
+		t.Error("a comment in the overlay changed the release fingerprint, so it would make a release of nothing (#514)")
+	}
+	if other := fingerprint("sha256:"+strings.Repeat("b", 64), none); other["fingerprint"] == first["fingerprint"] {
+		t.Error("a new image kept the release fingerprint, so the new build would never be released")
+	}
+	if again := fingerprint(a, "printf '%s\\n' 1.0.16-1 "+first["fingerprint"]); again["needed"] != "false" {
+		t.Errorf("a release of these exact manifests exists and another was needed: %v", again)
+	}
+}
+
+func runReleaseStepIn(t *testing.T, step, dir, path string, env []string) (ok bool, output, logs string) {
+	t.Helper()
+	return runReleaseStep(t, step, dir, path, env)
+}
diff --git a/tests/mutations.yml b/tests/mutations.yml
index 8a60266..765e3f5 100644
--- a/tests/mutations.yml
+++ b/tests/mutations.yml
@@ -85,7 +85,7 @@
 # require editing two places, and removing one should always require a visible,
 # reviewable act rather than a quiet deletion. Same shape as
 # tests/coverage-baseline.json, for the same reason.
-floor: 164
+floor: 166
 
 mutations:
   - guard: TestTheGameServerCannotBeGivenAPathIntoTheEstate
@@ -617,6 +617,27 @@ mutations:
       git name, which is not the image this build made - the release and its
       contents would disagree.
 
+  - guard: TestAnImageOfTheSameInputsIsReused
+    file: .github/workflows/fabricator.yml
+    find: 'printf ''%s\n'' "$PINS"; } | sha256sum'
+    replace: 'true; } | sha256sum'
+    mentions: "a new Steam build kept the old fingerprint"
+    why: >
+      A fingerprint that leaves out the pins reuses the old image when only a
+      pin moved - which is exactly what a new Steam build is. The expediter's
+      update would merge, the fabricator would reuse yesterday's image, and
+      production would never get the build players' clients now demand.
+
+  - guard: TestAReleaseOfTheSameManifestsIsNotRepeated
+    file: .github/workflows/fabricator.yml
+    find: 'fingerprint="fp-$(kubectl kustomize "${rendered}/${overlay}" | sha256sum | cut -c1-16)"'
+    replace: 'fingerprint="fp-$(cat "${rendered}/${overlay}/kustomization.yaml" | sha256sum | cut -c1-16)"'
+    mentions: "a comment in the overlay changed the release fingerprint"
+    why: >
+      Fingerprinting the files rather than what they render to counts a
+      comment as a change, which is #514 again: a release of nothing, and a
+      pull request for the operator asking to deploy it.
+
   - guard: TestEveryReleaseOrderIsOneTheFabricatorCanFollow
     file: environments/production/applications/valheim/kustomization.yaml
     find: "  - service.yaml\n"
