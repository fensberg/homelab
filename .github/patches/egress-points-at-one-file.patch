diff --git a/.github/workflows/clerk.yml b/.github/workflows/clerk.yml
index c1cf931..f5d0e31 100644
--- a/.github/workflows/clerk.yml
+++ b/.github/workflows/clerk.yml
@@ -113,6 +113,10 @@ jobs:
       - name: Harden the runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
diff --git a/.github/workflows/codeql.yml b/.github/workflows/codeql.yml
index 4a11a68..fdda01b 100644
--- a/.github/workflows/codeql.yml
+++ b/.github/workflows/codeql.yml
@@ -85,6 +85,10 @@ jobs:
       - name: Harden Runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
diff --git a/.github/workflows/dependency-review.yml b/.github/workflows/dependency-review.yml
index 03ee472..91e541a 100644
--- a/.github/workflows/dependency-review.yml
+++ b/.github/workflows/dependency-review.yml
@@ -56,6 +56,10 @@ jobs:
           # guessed, which is the same method the Semgrep lane's comment in
           # pr-validation.yml describes: run it, read what it actually
           # reached for, then allow exactly that.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.deps.dev:443
diff --git a/.github/workflows/deploy-infrastructure.yml b/.github/workflows/deploy-infrastructure.yml
index 6272199..4b9c4ed 100644
--- a/.github/workflows/deploy-infrastructure.yml
+++ b/.github/workflows/deploy-infrastructure.yml
@@ -88,6 +88,10 @@ jobs:
       - name: Harden Runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
@@ -203,6 +207,10 @@ jobs:
           # endpoints this reaches are not known until it has actually run
           # once, and a guessed allowlist on a job that touches real
           # infrastructure is worse than an audited one.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
 
       - name: Checkout Code
@@ -310,6 +318,10 @@ jobs:
           # the observed allowed-endpoints before this job ever runs for
           # real - not before, since a guessed allowlist on the one job with
           # write access to real infrastructure is worse than an audited one.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
 
       - name: Checkout Code
@@ -395,6 +407,10 @@ jobs:
           # API and the OpenTofu registry, and several of those addresses are
           # deliberately not in git. Transcribe the observed endpoints after
           # the first real run, then flip to block.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
 
       - name: Checkout Code
@@ -479,6 +495,10 @@ jobs:
       - name: Harden the runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
@@ -737,6 +757,10 @@ jobs:
       - name: Harden the runner (Audit all outbound calls)
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
 
       - name: Checkout Code
@@ -842,6 +866,10 @@ jobs:
       - name: Harden the runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
diff --git a/.github/workflows/expediter.yml b/.github/workflows/expediter.yml
index ab373ea..8466b1d 100644
--- a/.github/workflows/expediter.yml
+++ b/.github/workflows/expediter.yml
@@ -100,6 +100,10 @@ jobs:
       - name: Harden Runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
 
       - name: Checkout Code
diff --git a/.github/workflows/inspector.yml b/.github/workflows/inspector.yml
index e38c0cf..5c081b5 100644
--- a/.github/workflows/inspector.yml
+++ b/.github/workflows/inspector.yml
@@ -57,6 +57,10 @@ jobs:
       - name: Harden the runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
diff --git a/.github/workflows/integration-tests.yml b/.github/workflows/integration-tests.yml
index 251f15d..cc0512f 100644
--- a/.github/workflows/integration-tests.yml
+++ b/.github/workflows/integration-tests.yml
@@ -85,6 +85,10 @@ jobs:
           # checked - the same reasoning, and the same resolution, as
           # deploy-infrastructure.yml. Transcribe the observed destinations
           # after the first real run, then flip to block.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
 
       - name: Checkout Code
diff --git a/.github/workflows/pr-validation.yml b/.github/workflows/pr-validation.yml
index f1754ff..2c5f2d5 100644
--- a/.github/workflows/pr-validation.yml
+++ b/.github/workflows/pr-validation.yml
@@ -77,6 +77,10 @@ jobs:
         with:
           # pre-commit builds a Go toolchain for actionlint and a Node one
           # for prettier, which is why this list is the longest in the file.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
@@ -184,6 +188,10 @@ jobs:
         with:
           # action-shellcheck downloads the pinned shellcheck binary
           # straight from its GitHub release.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
@@ -241,6 +249,10 @@ jobs:
           # archive from storage.googleapis.com. dl.google.com, which used to
           # be listed here, appears nowhere in setup-go's source - a full
           # grep of every file that touches a URL found no reference to it.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
@@ -418,6 +430,10 @@ jobs:
           # lane, which already builds a Node toolchain for prettier under
           # block. Nothing here is new traffic - it is two existing lanes'
           # traffic in one job.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
@@ -598,6 +614,10 @@ jobs:
           # bundled Go is older than go.mod's pinned version (scripts/contractor
           # pins go 1.26, deliberately - see the root CLAUDE.md - so this is
           # not a version to relax to dodge the network requirement).
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
@@ -662,6 +682,10 @@ jobs:
           # rubber-stamping an allowlist nobody checked. See the Format lane
           # above for how that list gets built the right way: run audit
           # first, then transcribe the real destinations it logged.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
 
       - name: Checkout Code
@@ -709,6 +733,10 @@ jobs:
         with:
           # Trivy pulls its misconfiguration database as an OCI artifact,
           # from ghcr.io with a Google mirror as fallback.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
@@ -773,6 +801,10 @@ jobs:
           # leaked key belongs to whoever it belongs to. An allowlist here
           # would not fail loudly; it would quietly turn verified findings into
           # unverified ones, which is the opposite of what this lane is for.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
 
       - name: Checkout Code
diff --git a/.github/workflows/runner-image.yml b/.github/workflows/runner-image.yml
index 329ddb5..81d4e1c 100644
--- a/.github/workflows/runner-image.yml
+++ b/.github/workflows/runner-image.yml
@@ -54,6 +54,10 @@ jobs:
       - name: Harden Runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
 
       - name: Checkout Code
diff --git a/.github/workflows/scorecard.yml b/.github/workflows/scorecard.yml
index d34ec0f..a014888 100644
--- a/.github/workflows/scorecard.yml
+++ b/.github/workflows/scorecard.yml
@@ -48,6 +48,10 @@ jobs:
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
           # Scorecard queries several public datasets to build its grade.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.deps.dev:443
diff --git a/.github/workflows/security-patrol.yml b/.github/workflows/security-patrol.yml
index f693810..a4d1989 100644
--- a/.github/workflows/security-patrol.yml
+++ b/.github/workflows/security-patrol.yml
@@ -81,6 +81,10 @@ jobs:
       - name: Harden Runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
diff --git a/.github/workflows/sensitive-paths.yml b/.github/workflows/sensitive-paths.yml
index 6a37c23..86837e1 100644
--- a/.github/workflows/sensitive-paths.yml
+++ b/.github/workflows/sensitive-paths.yml
@@ -67,6 +67,10 @@ jobs:
       - name: Harden the runner
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
         with:
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: block
           allowed-endpoints: >
             api.github.com:443
diff --git a/.github/workflows/workload-images.yml b/.github/workflows/workload-images.yml
index 0a2f0c3..d6192f5 100644
--- a/.github/workflows/workload-images.yml
+++ b/.github/workflows/workload-images.yml
@@ -65,6 +65,10 @@ jobs:
           # that is pinned by digest and pulled from this estate's own registry,
           # so what reaches the cluster is a specific set of bytes somebody can
           # point at, whatever the build did to obtain them.
+          # Owned by scripts/approved-suppliers.yml under `egress:`, which says
+          # which job may reach what; `security guard-egress` refuses any drift.
+          # The copy lives here because harden-runner reads its policy in a
+          # pre-step, before any step of this job can run.
           egress-policy: audit
         uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
 
