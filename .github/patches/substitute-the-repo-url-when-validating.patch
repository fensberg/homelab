diff --git a/.github/workflows/pr-validation.yml b/.github/workflows/pr-validation.yml
index 644189e..07f1dfd 100644
--- a/.github/workflows/pr-validation.yml
+++ b/.github/workflows/pr-validation.yml
@@ -321,6 +321,9 @@ jobs:
           STATE_DB_NODEPORT: "30432"
           OBJECT_STORAGE_BUCKET: placeholder-bucket
           OBJECT_STORAGE_ENDPOINT: https://example.invalid
+          # A stand-in, like the rest: only the shape is checked, and the real
+          # URL belongs in the config rather than in a workflow.
+          SOURCE_CONTROL_REPO_URL: https://example.invalid/fork/homelab
         run: |
           schema='https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json'
           for dir in clusters/management/infrastructure/*/ clusters/management/flux-system/; do
