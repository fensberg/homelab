diff --git a/.github/workflows/deploy-infrastructure.yml b/.github/workflows/deploy-infrastructure.yml
index 4b9c4ed..b6b2e44 100644
--- a/.github/workflows/deploy-infrastructure.yml
+++ b/.github/workflows/deploy-infrastructure.yml
@@ -437,9 +437,18 @@ jobs:
           OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
           SITE: ${{ matrix.site }}
         run: |
-          go build -C scripts/contractor -o contractor .
+          # toolshed/, like everything else that builds here.
+          #
+          # This built to scripts/contractor/contractor while the taskfile built
+          # to toolshed/contractor, so one program had two build outputs
+          # depending on who was building it (#215). Both worked; together they
+          # meant the program printed CI's path to somebody standing at a
+          # workstation, in the message telling them how to recover from a total
+          # loss. Four Go messages and three documents had drifted onto the CI
+          # path, and a real ignition printed one of them.
+          go build -C scripts/contractor -o ../../toolshed/contractor .
           set +e
-          ./scripts/contractor/contractor converge -site "$SITE"
+          ./toolshed/contractor converge -site "$SITE"
           code=$?
           set -e
           # The verdict, not just the failure. contractor exits 2 when the
@@ -783,8 +792,8 @@ jobs:
           OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
           SITE: ${{ matrix.site }}
         run: |
-          go build -C scripts/contractor -o contractor .
-          ./scripts/contractor/contractor plan -site "$SITE" -comment-out plan-comment.md
+          go build -C scripts/contractor -o ../../toolshed/contractor .
+          ./toolshed/contractor plan -site "$SITE" -comment-out plan-comment.md
 
       - name: Comment the plan on the pull request
         uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
diff --git a/.github/workflows/integration-tests.yml b/.github/workflows/integration-tests.yml
index cc0512f..5e9fcd8 100644
--- a/.github/workflows/integration-tests.yml
+++ b/.github/workflows/integration-tests.yml
@@ -118,8 +118,8 @@ jobs:
           OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
           SITE: ${{ inputs.site || 'site0' }}
         run: |
-          go build -C scripts/contractor -o contractor .
-          ./scripts/contractor/contractor break-ground -site "$SITE" -phase render -keep-on-failure
+          go build -C scripts/contractor -o ../../toolshed/contractor .
+          ./toolshed/contractor break-ground -site "$SITE" -phase render -keep-on-failure
 
       - name: API contract tests
         if: ${{ inputs.tier == 'api' || inputs.tier == 'both' }}
@@ -159,7 +159,7 @@ jobs:
         env:
           OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
           SITE: ${{ inputs.site || 'site0' }}
-        run: ./scripts/contractor/contractor break-ground -site "$SITE" -phase backup -keep-on-failure
+        run: ./toolshed/contractor break-ground -site "$SITE" -phase backup -keep-on-failure
 
       # Run through the contractor, not beside it.
       #
@@ -187,7 +187,7 @@ jobs:
           HOMELAB_TEST_SITE: ${{ inputs.site || 'site0' }}
           SITE: ${{ inputs.site || 'site0' }}
         run: >-
-          ./scripts/contractor/contractor kubeconfig -site "$SITE" --
+          ./toolshed/contractor kubeconfig -site "$SITE" --
           go test -C tests/go -tags=integration -timeout 20m -v ./integration/...
 
       # always(), because the whole point is that no secret survives this job
@@ -198,4 +198,4 @@ jobs:
         if: always()
         env:
           SITE: ${{ inputs.site || 'site0' }}
-        run: ./scripts/contractor/contractor break-ground -site "$SITE" -phase sterilize
+        run: ./toolshed/contractor break-ground -site "$SITE" -phase sterilize
