diff --git a/.github/workflows/expediter.yml b/.github/workflows/expediter.yml
index 51a9743..ab373ea 100644
--- a/.github/workflows/expediter.yml
+++ b/.github/workflows/expediter.yml
@@ -56,12 +56,12 @@ on:
     # hours - so treat this as "within a few hours" rather than fifteen
     # minutes, and dispatch it by hand when a patch has just landed.
     - cron: "*/15 * * * *"
+  # A dispatch by hand always takes a delivery, even of a build already
+  # pinned: after a Dockerfile change, or when Valve has just published and the
+  # scheduler is hours behind. Deliberately no inputs - Checkov's CKV_GHA_7
+  # refuses any, because an input lets whoever dispatches steer what is built,
+  # and the event itself already says everything this needs to know.
   workflow_dispatch:
-    inputs:
-      force:
-        description: "Take a delivery even if Steam's build is unchanged (after a Dockerfile change)"
-        type: boolean
-        default: false
 
 permissions:
   contents: read
@@ -110,7 +110,7 @@ jobs:
       - name: Check deliveries
         id: check
         env:
-          FORCE: ${{ inputs.force }}
+          FORCE: ${{ github.event_name == 'workflow_dispatch' }}
         run: |
           set -euo pipefail
 
@@ -149,9 +149,11 @@ jobs:
             exit 0
           fi
           echo "delivery: Steam build ${build}, replacing ${recorded:-nothing}" >> "$GITHUB_STEP_SUMMARY"
-          echo "deliver=true" >> "$GITHUB_OUTPUT"
-          echo "build=${build}" >> "$GITHUB_OUTPUT"
-          echo "recorded=${recorded}" >> "$GITHUB_OUTPUT"
+          {
+            echo "deliver=true"
+            echo "build=${build}"
+            echo "recorded=${recorded}"
+          } >> "$GITHUB_OUTPUT"
 
       - name: Take delivery
         id: image
@@ -191,6 +193,14 @@ jobs:
         with:
           client-id: ${{ secrets.EXPEDITER_BOT_APP_ID }}
           private-key: ${{ secrets.EXPEDITER_BOT_PRIVATE_KEY }}
+          # Only what the steps below use, not everything the App was granted
+          # at installation (zizmor's github-app audit). Contents to commit,
+          # merge and tag; pull requests to open, comment on and merge; checks
+          # and statuses to watch the required checks.
+          permission-contents: write
+          permission-pull-requests: write
+          permission-checks: read
+          permission-statuses: read
           # This repository only - the token cannot reach anything else the
           # App happens to be installed on.
           owner: ${{ github.repository_owner }}
