diff --git a/.github/workflows/security-patrol.yml b/.github/workflows/security-patrol.yml
index 775ac5d..79c2d45 100644
--- a/.github/workflows/security-patrol.yml
+++ b/.github/workflows/security-patrol.yml
@@ -57,7 +57,9 @@ on:
   workflow_dispatch:
 
 permissions:
-  actions: read
+  # read to ask what is queued; write to cancel a run whose branch is gone
+  # (#181). Nothing else here writes anything.
+  actions: write
   contents: read
 
 concurrency:
@@ -102,6 +104,20 @@ jobs:
       - name: Setup Go
         uses: $/.github/workflows/setup-go
 
+      # Cancel what can never run, before asking whether work is being picked
+      # up. A queued run does not die with its branch: two plans on a merged,
+      # deleted branch sat in the queue until an ignition refused to start
+      # because of them (#181). Only a run whose branch is gone is cancelled -
+      # proof rather than a guess - and a run waiting on an approval is never
+      # touched, because that is a person rather than a fault.
+      - name: Reap queued runs that can never produce a result
+        env:
+          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
+          GITHUB_REPOSITORY: ${{ github.repository }}
+        run: |
+          go build -C scripts/security -o ../../toolshed/security .
+          ./toolshed/security reap-queue -confirm | tee -a "$GITHUB_STEP_SUMMARY"
+
       - name: Ask whether the estate is answering for itself
         env:
           # Read-only, and scoped to this repository by GitHub. The canary
