name: "PR Validation"

on:
  # No push: trigger to main. The ruleset there has bypass_actors: [],
  # current_user_can_bypass: never, and requires a pull_request - a direct
  # push cannot land, so a push-to-main run would only ever re-validate
  # content this same workflow already validated as a pull_request seconds
  # earlier. If that ruleset is ever loosened to allow a bypass, reconsider
  # this then.
  pull_request:
    branches:
      - main

permissions: read-all

# Cancel in-progress runs if a new commit is pushed to the PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# All lanes run in parallel, Format included. Formatting is enforced by the
# git hook `install-dependencies.sh` wires up (`pre-commit install`), which is
# where it belongs - shift left, catch it in seconds on the machine that wrote
# it, not minutes later in CI. Format's own lane here exists only as the
# backstop for whoever's local hook is missing or bypassed: a first-time
# contributor who never ran the setup script, a bot PR that never touches a
# git hook at all. Gating every other lane on Format used to buy a faster
# failure for a formatting slip, but it also meant a bot or outside PR with
# nothing wrong except formatting would never even get its other lanes run -
# and their results would land staggered by however long Format took, rather
# than together. Running everything at once costs a bit more compute on a
# branch that turns out to have a formatting slip, in exchange for every lane
# - Format included - actually running for every PR, and their results
# landing together.
#
# NOTHING IS REMOVED beyond that, and nothing should be: Semgrep, Trivy and
# checkov overlap on purpose. They are an independent check on code an LLM
# helped write, so a scanner that finds nothing this week is not redundant -
# it is the one that catches what nobody thought to look for.

jobs:
  # -----------------------------------------------------------------
  # LANE 1: Formatting - fast, and the only lane that also runs locally
  # -----------------------------------------------------------------
  format:
    name: "Format (pre-commit)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # pre-commit builds a Go toolchain for actionlint and a Node one
          # for prettier, which is why this list is the longest in the file.
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            codeload.github.com:443
            dl.google.com:443
            files.pythonhosted.org:443
            github.com:443
            go.dev:443
            nodejs.org:443
            objects.githubusercontent.com:443
            proxy.golang.org:443
            pypi.org:443
            raw.githubusercontent.com:443
            registry.npmjs.org:443
            release-assets.githubusercontent.com:443
            storage.googleapis.com:443
            sum.golang.org:443

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # The same .pre-commit-config.yaml a developer runs, at the same pinned
      # versions. That is the point: there is no second copy of prettier here
      # to disagree with the one on your machine.
      #
      # tofu-fmt and go-fmt are skipped because both are system hooks with no
      # pinned binary on this runner - ubuntu-latest ships some Go toolchain,
      # but not necessarily the one this repo pins. The Validate lane installs
      # a pinned tofu and runs `tofu fmt -check` itself; Go formatting is
      # enforced later too, by Super-Linter's Go analysis in the Analyze lane.
      - name: Run pre-commit
        env:
          SKIP: tofu-fmt,go-fmt
        run: |
          pipx install pre-commit==4.6.2
          pre-commit run --all-files --show-diff-on-failure --color always

  # -----------------------------------------------------------------
  # LANE 2: Shell scripts - the sole owner of ShellCheck. Super-Linter
  # bundles its own shellcheck invocation (BASH), switched off in
  # super-linter.vars for the same one-owner-per-check reason Trivy was
  # pulled out: this repo has two shell scripts, so a dedicated lane costs
  # nothing and is one less shared version/config surface to keep in sync.
  # BASH_EXEC (execute-bit checking) stays in Super-Linter - it isn't
  # shellcheck and doesn't overlap with this.
  # -----------------------------------------------------------------
  shellcheck:
    name: "Shell Lint (ShellCheck)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # action-shellcheck downloads the pinned shellcheck binary
          # straight from its GitHub release.
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            github.com:443
            objects.githubusercontent.com:443
            release-assets.githubusercontent.com:443

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@00cae500b08a931fb5698e11e79bfbd38e612a38 # 2.0.0
        with:
          # Pinned to match the shellcheck version Super-Linter slim-v8.7.0
          # bundles, so a lint that passes here would also have passed
          # there - one version to reason about, not two.
          version: "v0.11.0"
          check_together: "yes"

  # -----------------------------------------------------------------
  # LANE 3: Does it actually work?
  # -----------------------------------------------------------------
  # Linting says the code is tidy. This says it is correct: that the OpenTofu
  # resolves, that the config template still carries every key the OpenTofu
  # reads, and that the manifests Flux will apply are valid Kubernetes - which
  # no linter could tell, because on disk they are templates full of ${...}
  # placeholders.
  validate:
    name: "Validate (tofu, kustomize, kubeconform, go)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # get.opentofu.org serves both the installer and the GPG key it
          # verifies the release with. Provider downloads start at the registry
          # and are redirected to GitHub release assets. The schema fetches for
          # kubeconform come from raw.githubusercontent.com.
          #
          # actions/setup-go (confirmed from its own source at this pinned
          # commit, not assumed) has two real, different paths depending on
          # whether the hosted actions/go-versions manifest resolves: the
          # normal one walks api.github.com's git/trees + git/blobs endpoints
          # then downloads from github.com's releases, redirected to
          # release-assets.githubusercontent.com - confirmed as the actual
          # path taken in a real run. The fallback, unexercised so far but
          # real in the source, lists versions from go.dev and downloads the
          # archive from storage.googleapis.com. dl.google.com, which used to
          # be listed here, appears nowhere in setup-go's source - a full
          # grep of every file that touches a URL found no reference to it.
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            get.opentofu.org:443
            github.com:443
            go.dev:443
            objects.githubusercontent.com:443
            raw.githubusercontent.com:443
            registry.opentofu.org:443
            release-assets.githubusercontent.com:443
            storage.googleapis.com:443

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Versions come from scripts/versions.env, which is the only place one
      # is written. See .github/actions/versions for why this is a step
      # rather than a setup job.
      - name: Read pinned versions
        uses: ./.github/actions/versions

      - name: Install OpenTofu
        run: |
          curl --proto '=https' --tlsv1.2 -fsSL \
            https://get.opentofu.org/install-opentofu.sh -o install-opentofu.sh
          chmod +x install-opentofu.sh
          ./install-opentofu.sh --install-method standalone \
            --opentofu-version "${TOFU_VERSION}"
          rm install-opentofu.sh

      # CI has no 1Password credentials and must not have any. The rendered
      # config is a stand-in with the same shape, which is enough for validate
      # and is itself a check: it fails if the template stops being valid JSON
      # once injected, or stops carrying a key the OpenTofu reads.
      - name: Render a placeholder config
        run: bash .github/scripts/placeholder-config.sh

      - name: Check OpenTofu formatting
        run: tofu fmt -check -recursive management/

      - name: Validate OpenTofu
        working-directory: management/cluster
        env:
          # The placeholder render writes its own filename, never the Render
          # phase's, so nothing here can collide with a real run. `tofu
          # validate` takes no -var flag, hence the environment variable.
          TF_VAR_config_path: ../../config/management.placeholder.json
        run: |
          tofu init -backend=false -input=false
          tofu validate

      # `tofu test` used to run here. It moved to the Test lane below, along
      # with `go test`: this lane answers "does it resolve", that one answers
      # "does it behave", and one owner per check is the rule everywhere else
      # in this file too.

      - name: Install kubeconform
        run: |
          curl -fsSL -o kubeconform.tar.gz \
            "https://github.com/yannh/kubeconform/releases/download/v${KUBECONFORM_VERSION}/kubeconform-linux-amd64.tar.gz"
          tar xzf kubeconform.tar.gz kubeconform
          sudo install -m 0755 kubeconform /usr/local/bin/kubeconform
          rm kubeconform.tar.gz kubeconform

      # Build the kustomization and substitute the placeholders before
      # validating, so what is checked is what Flux will apply. The values are
      # stand-ins; only their shape matters, and shape is exactly what was
      # going unchecked - a nodePort has to be an integer, and on disk it is
      # the string "${STATE_DB_NODEPORT}".
      - name: Validate Kubernetes manifests
        env:
          STATE_DB_NAMESPACE: state
          STATE_DB_CLUSTER: state-db
          STATE_DB_NAME: tfstate
          STATE_DB_OWNER: tofu
          STATE_DB_NODEPORT: "30432"
          OBJECT_STORAGE_BUCKET: placeholder-bucket
          OBJECT_STORAGE_ENDPOINT: https://example.invalid
        run: |
          schema='https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json'
          for dir in clusters/management/infrastructure/*/ clusters/management/flux-system/; do
            echo "==> ${dir}"
            kubectl kustomize "${dir}" | envsubst | kubeconform \
              -strict -summary -ignore-missing-schemas \
              -schema-location default -schema-location "${schema}" -
          done

          echo "==> clusters/management (Flux entrypoints)"
          kubeconform -strict -summary -ignore-missing-schemas \
            -schema-location default -schema-location "${schema}" \
            clusters/management/infra-controllers.yaml \
            clusters/management/infra-configs.yaml

      # Not Super-Linter's Go linting: in diff mode, when a PR's changed Go
      # files span more than one package directory, Super-Linter hands them
      # to golangci-lint as a flat file list, which go/packages rejects with
      # "named files must all be in one directory" - a confirmed upstream
      # bug (super-linter/super-linter#6294), not anything about this code.
      # go vet/build here works on whole packages, so it is not exposed to
      # that bug, and it is not diff-limited either. VALIDATE_GO and
      # VALIDATE_GO_MODULES are off in super-linter.vars accordingly.
      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: ${{ env.GO_VERSION }}
          # No go.sum: scripts/steward has zero external dependencies, so
          # there is nothing meaningful to cache.
          cache: false

      - name: Validate Go
        working-directory: scripts/steward
        run: |
          go vet ./...
          go build -o /dev/null .

      # The test-only module is deliberately NOT vetted here. It has real
      # dependencies, so vetting it needs proxy.golang.org, and this lane's
      # egress allowlist does not carry it - on purpose. scripts/steward has
      # no dependencies, and a Validate lane that cannot reach a module proxy
      # at all is the cheapest possible proof of that. It is vetted in the
      # Test lane instead, which already has both the endpoints and the
      # module cache.

  # -----------------------------------------------------------------
  # LANE 4: Does it behave?
  # -----------------------------------------------------------------
  # Validate above proves the code resolves. This proves it does the right
  # thing: the Go unit tests, the config-contract tests that hold
  # scripts/steward and management/cluster/registry.tf to the same rules, the
  # OpenTofu invariant tests, and the JavaScript/TypeScript tier.
  #
  # Everything here is hermetic - no 1Password, no hypervisor, no cluster.
  # The tiers that need a real estate live in integration-tests.yml, on a
  # self-hosted runner, and cannot be triggered by a pull request. That
  # separation is the point: a fork PR runs this lane in full and still cannot
  # reach a credential or a machine.
  #
  # One lane rather than three because the three steps share a checkout and
  # most of a toolchain, and splitting them would triple the install time to
  # buy a slightly more specific red X. The step names carry that information
  # already.
  test:
    name: "Test (go, tofu, vitest)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # Not a guessed list: every entry here is already proven under
          # egress-policy: block by another lane in this same file. The
          # OpenTofu and Go entries are the Validate lane's list verbatim
          # (same installer, same setup-go, same provider downloads); the
          # nodejs.org and registry.npmjs.org entries come from the Format
          # lane, which already builds a Node toolchain for prettier under
          # block. Nothing here is new traffic - it is two existing lanes'
          # traffic in one job.
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            get.opentofu.org:443
            github.com:443
            go.dev:443
            nodejs.org:443
            objects.githubusercontent.com:443
            proxy.golang.org:443
            registry.npmjs.org:443
            registry.opentofu.org:443
            release-assets.githubusercontent.com:443
            storage.googleapis.com:443
            sum.golang.org:443

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Versions come from scripts/versions.env, which is the only place one
      # is written. See .github/actions/versions for why this is a step
      # rather than a setup job.
      - name: Read pinned versions
        uses: ./.github/actions/versions

      # --- Go -------------------------------------------------------------
      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: ${{ env.GO_VERSION }}
          # tests/go is the only module here with dependencies to cache;
          # scripts/steward has none, so there is no go.sum of its own to key on.
          cache-dependency-path: tests/go/go.sum

      - name: Unit and contract tests (Go)
        working-directory: scripts/steward
        run: go test -covermode=atomic -coverprofile=coverage-go.out ./...

      # Every build tag, because `go vet ./...` compiles none of the tiers
      # that carry one. A compile error in the integration or e2e tier should
      # surface on the pull request that caused it, not the night someone
      # first reaches for that suite.
      - name: Vet the test module (every tier)
        working-directory: tests/go
        run: |
          go vet ./...
          go vet -tags=api ./...
          go vet -tags=integration ./...
          go vet -tags=e2e ./...

      # Reported on every run, enforced as a ratchet: a pull request may not
      # push coverage below the committed floor, and is free to leave it
      # exactly where it found it. See tests/coverage-baseline.json.
      - name: Coverage gate (Go)
        run: |
          pct="$(go -C scripts/steward tool cover -func=coverage-go.out \
            | awk '/^total:/ {print substr($3, 1, length($3)-1)}')"
          bash scripts/coverage-gate.sh go "$pct"

      # --- OpenTofu --------------------------------------------------------
      - name: Install OpenTofu
        run: |
          curl --proto '=https' --tlsv1.2 -fsSL \
            https://get.opentofu.org/install-opentofu.sh -o install-opentofu.sh
          chmod +x install-opentofu.sh
          ./install-opentofu.sh --install-method standalone \
            --opentofu-version "${TOFU_VERSION}"
          rm install-opentofu.sh

      # Fixture configs from management/cluster/tests/fixtures, never the real
      # rendered one, so this needs no credentials. The Go contract tests
      # above assert that this corpus and the Go one are the same corpus - a
      # case added to one side and forgotten on the other fails there.
      - name: Invariant tests (OpenTofu)
        working-directory: management/cluster
        run: |
          tofu init -backend=false -input=false
          tofu test

      # Untagged, so this compiles and runs only the hermetic tests in
      # tests/go: the repository-invariant checks and the state-encryption
      # cutover rehearsal. The integration, api and e2e tiers all carry a
      # build tag and are not reachable here.
      #
      # After the OpenTofu install above, not before - the cutover rehearsal
      # drives a real `tofu` against a scratch workspace.
      - name: Repository invariants and the encryption rehearsal
        working-directory: tests/go
        run: go test ./...

      # --- JavaScript / TypeScript ----------------------------------------
      # Before setup-node, so that setup-node's pnpm cache has a pnpm to
      # find. The version comes from package.json's packageManager field, so
      # the pin lives with the project rather than in this file.
      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile

      # The type checker, not a linter: it reads the same tsconfig.json a
      # developer's editor does, and it is the owner of TypeScript
      # correctness here - see .github/super-linter.vars.
      - name: Typecheck (tsc)
        run: pnpm typecheck

      - name: Unit tests (vitest)
        run: pnpm test:coverage

      - name: Coverage gate (JS)
        run: |
          pct="$(node -e 'const p=require("./coverage/js/coverage-summary.json").total.statements.pct;console.log(Number.isFinite(p)?p:0)' 2>/dev/null || echo 0)"
          bash scripts/coverage-gate.sh js "$pct"

  # -----------------------------------------------------------------
  # LANE 5: Static analysis - the slow one
  # -----------------------------------------------------------------
  # checkov, ansible-lint, tflint, shellcheck, markdownlint, yamllint,
  # PSScriptAnalyzer, zizmor. Every formatter it would otherwise duplicate is
  # switched off in .github/super-linter.vars; the reasoning lives there.
  analyze:
    name: "Analyze (Super-Linter)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      statuses: write
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # The slim image carries its own linters, but three of them fetch
          # data at run time rather than shipping it: Super-Linter bundles
          # Trivy, which pulls its vulnerability database as an OCI artifact
          # from mirror.gcr.io (redirected to storage.googleapis.com);
          # checkov checks pypi on startup; and the Go linters auto-download
          # a matching toolchain via GOPROXY/GOSUMDB whenever the image's
          # bundled Go is older than go.mod's pinned version (scripts/steward
          # pins go 1.26, deliberately - see the root CLAUDE.md - so this is
          # not a version to relax to dodge the network requirement).
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            files.pythonhosted.org:443
            ghcr.io:443
            github.com:443
            mirror.gcr.io:443
            objects.githubusercontent.com:443
            pkg-containers.githubusercontent.com:443
            proxy.golang.org:443
            pypi.org:443
            raw.githubusercontent.com:443
            registry.npmjs.org:443
            storage.googleapis.com:443
            sum.golang.org:443

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      # The same file `task lint` passes to the container, so a local run and
      # CI are configured identically. See .github/super-linter.vars.
      - name: Load Super-Linter configuration
        run: grep -v "^#" .github/super-linter.vars | grep . >> "$GITHUB_ENV"

      - name: Run Super-Linter
        uses: super-linter/super-linter/slim@4ce20838b8ab83717e78138c5b3a1407148e0918 # v8.7.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # CI lints the diff. A local run lints everything, because RUN_LOCAL
          # forces it - which is why this is here and not in the shared file.
          VALIDATE_ALL_CODEBASE: false

  # -----------------------------------------------------------------
  # LANE 6: Static Application Security Testing (SAST)
  # -----------------------------------------------------------------
  semgrep:
    name: "Logic Scan (Semgrep)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    container:
      # Pinned by digest for Scorecard's Pinned-Dependencies check. Nothing
      # bumps this automatically - Dependabot's docker ecosystem covers
      # Dockerfiles/compose files, not an image reference embedded in a
      # workflow's container: block - so this needs a manual version+digest
      # refresh occasionally.
      image: semgrep/semgrep:1.172.0@sha256:65dcd4408adda7c183a6b4550cb1e9b19f7f627a6fbb7e0559bd466bedc44d7b
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # Stays audit, not block, until a real run's egress log gives an
          # allowed-endpoints list worth trusting - semgrep ci reaches its
          # rule registry at a set of hosts this repo has never actually
          # observed, and guessing them risks either breaking the scan or
          # rubber-stamping an allowlist nobody checked. See the Format lane
          # above for how that list gets built the right way: run audit
          # first, then transcribe the real destinations it logged.
          egress-policy: audit

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Run Semgrep CI
        run: semgrep ci
        env:
          SEMGREP_RULES: "p/default"

  # -----------------------------------------------------------------
  # LANE 7: Infrastructure Misconfigurations + Dependency Vulnerabilities
  # (IaC + vuln) - the sole owner of Trivy. Super-Linter bundles its own
  # Trivy invocation, but VALIDATE_TRIVY is switched off in
  # super-linter.vars so it never runs there: two Trivy invocations with
  # two different configs is exactly the failure mode that put an
  # unformatted 11k-line file on a PR that pre-commit had already passed
  # (see the "owned by" comments in super-linter.vars). scan-type "fs"
  # with scanners vuln+misconfig covers what Super-Linter's bundled Trivy
  # was doing (gomod vulnerability scanning + Kubernetes/Terraform
  # misconfiguration scanning) in a single pass; secret scanning is
  # deliberately left off scanners here because Secrets Scan (TruffleHog)
  # already owns that.
  # -----------------------------------------------------------------
  trivy:
    name: "IaC Scan (Trivy)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # Trivy pulls its misconfiguration database as an OCI artifact,
          # from ghcr.io with a Google mirror as fallback.
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            ghcr.io:443
            github.com:443
            mirror.gcr.io:443
            objects.githubusercontent.com:443
            pkg-containers.githubusercontent.com:443
            release-assets.githubusercontent.com:443

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Run Trivy Vulnerability Scanner
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: "fs"
          scanners: "vuln,misconfig"
          hide-progress: false
          format: "table"
          exit-code: "1"
          severity: "CRITICAL,HIGH"
          # clusters/management/flux-system/ is Flux's own generated install
          # manifest, committed verbatim (see super-linter.vars for the full
          # reasoning). Its ClusterRoles need broad permissions to reconcile
          # arbitrary resource types - that is how Flux has to work, not a
          # misconfiguration in code we control - so this is excluded from
          # scanning entirely rather than suppressing the specific rule IDs
          # project-wide, which would also silence real findings elsewhere.
          skip-dirs: "clusters/management/flux-system"

  # -----------------------------------------------------------------
  # LANE 8: Secrets - waits for nothing
  # -----------------------------------------------------------------
  secrets:
    name: "Secrets Scan (TruffleHog)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # DELIBERATELY audit, not block. TruffleHog verifies a candidate
          # secret by presenting it to the API of whichever vendor issued it,
          # and there is no way to know in advance which vendors those are - a
          # leaked key belongs to whoever it belongs to. An allowlist here
          # would not fail loudly; it would quietly turn verified findings into
          # unverified ones, which is the opposite of what this lane is for.
          egress-policy: audit

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      # Versions come from scripts/versions.env, which is the only place one
      # is written. See .github/actions/versions for why this is a step
      # rather than a setup job.
      - name: Read pinned versions
        uses: ./.github/actions/versions

      # Not the marketplace action: its own action.yml runs
      # `docker run "${IMAGE}:${VERSION}"` with VERSION defaulting to
      # `latest` and no input that can express a digest (the template is
      # hardcoded to `image:tag`, never `image@sha256:...`). SHA-pinning the
      # action only pins its orchestration script - it still silently pulls
      # whatever `latest` resolves to today on the one job in this repo
      # that's deliberately not egress-blocked. Installing the CLI directly,
      # pinned and checksum-verified, closes that gap the same way tofu and
      # kubeconform already are in the Validate lane above.
      - name: Install trufflehog (pinned, checksum-verified)
        run: |
          tarball="trufflehog_${TRUFFLEHOG_VERSION}_linux_amd64.tar.gz"
          curl -fsSLO "https://github.com/trufflesecurity/trufflehog/releases/download/v${TRUFFLEHOG_VERSION}/${tarball}"
          echo "${TRUFFLEHOG_SHA256}  ${tarball}" | sha256sum -c -
          tar -xzf "${tarball}" trufflehog
          sudo install -m 0755 trufflehog /usr/local/bin/trufflehog

      # Same flags the marketplace action passed internally. base/head come
      # straight from the pull_request event - this workflow no longer has a
      # push trigger (see the on: block above), so that is the only case
      # worth handling.
      - name: Run TruffleHog
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          trufflehog git "file://$(pwd)" \
            --since-commit "${BASE_SHA}" \
            --branch "${HEAD_SHA}" \
            --fail --no-update --github-actions --results=verified,unknown
