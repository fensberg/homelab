---
all:
  children:
    hypervisors:
      hosts:
        "{{ op://homelab/hypervisor/node0_hostname }}":
          ansible_host: "{{ op://homelab/hypervisor/node0_ip }}"
          ansible_user: root
